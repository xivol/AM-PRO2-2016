#pragma once
class test_list
{
    bool equals(const list &l1, const list &l2);
    bool copy_ctor();
    bool assign();
    bool push_back();
    bool pop_back();
    bool back();
public:
    bool run();
};

